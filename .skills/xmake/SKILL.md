---
name: xmake-build-engine
description: Skills for managing C++ compilation, xrepo packages, and CI/CD via Xmake.
---

# Xmake Engine Rules
- Refer to the official rule sets maintained in `./.skills/xmake-skills/` for syntax guidelines and best practices.
- Load these skills automatically whenever working with xmake-related tasks, modifying `xmake.lua`, or investigating build issues.

# Executable Tool Mappings
- To build the project, execute: `xmake`
- To update dependencies, execute: `xmake require --upgrade`
- To run tests, execute: `xmake test`
